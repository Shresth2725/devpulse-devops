pipeline {
    agent any

    stages {
        stage('Hello Jenkins') {
            steps {
                sh 'echo Jenkins is working'
            }
        }
        stage('Verify Ansible') {
            steps {
                sh 'ansible --version'
            }
        }
    }
}
